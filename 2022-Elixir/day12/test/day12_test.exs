defmodule Day12Test do
  use ExUnit.Case
  doctest Day12

  @grid %{
    {0, 0} => ?p,
    {1, 0} => ?q,
    {2, 0} => ?r,
    {3, 0} => ?s,
    {0, 1} => ?w,
    {1, 1} => ?v,
    {2, 1} => ?u,
    {3, 1} => ?t,
    {0, 2} => ?x,
    {1, 2} => ?y,
    {2, 2} => ?z,
    {3, 2} => ?E
  }

  # pqrs
  # wvut
  # xyzE
end
